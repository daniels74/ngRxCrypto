import { BorderglowDirective } from './borderglow.directive';

describe('BorderglowDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderglowDirective();
    expect(directive).toBeTruthy();
  });
});
